{
	"type":"date",
	"text":"${date.getText()}",
	"from":<#if date.from ? exists> "${date.from }"<#else> ""</#if>,
	"to":<#if date.to ? exists> "${date.to }"<#else> ""</#if>,
	"unit":"${date.getUnitStrOfDate()}",
	"dateLength":"${date.getLength()}",
	"isSequence":"${date.isSequence()?string("true","false")}"
}